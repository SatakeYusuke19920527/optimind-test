import React from 'react';

// components
import Layout from '../components/Layout';

const Login: React.FC<{}> = () => {
  return (
    <Layout>
      <h1>Login</h1>
    </Layout>
  );
};

export default Login;
